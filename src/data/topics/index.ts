import { algebraTopic } from './algebra';
import { geometriTopic } from './geometri';
import { prosentTopic } from './prosent';
import { statistikkTopic } from './statistikk';
import { funksjonerTopic } from './funksjoner';
import { potenserTopic } from './potenser';
import { solsystemetTopic } from './solsystemet';
import type { Topic } from '../../types';

export const topics: Topic[] = [
  algebraTopic,
  geometriTopic,
  prosentTopic,
  statistikkTopic,
  funksjonerTopic,
  potenserTopic,
  solsystemetTopic,
];
